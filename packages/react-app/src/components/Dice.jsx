
import React, { useState } from "react";
import { useBalance } from "eth-hooks";

const { utils } = require("ethers");

export default function Dice() {
    let dice = "";
    console.log("in dice baby!")
    dice = (
        <div > 
        dice yo
        </div>
      );
    return dice;
}


